# AI Interview Agent — 项目通用设置

> 每次会话自动加载。Claude 必须以本文件中的约定为基础执行所有任务。

---

## 项目概述

AI 驱动的技术面试平台。后端用 FastAPI 编排面试状态机 + LLM 对话，前端用 Next.js 16 提供仪表盘和实时面试聊天室。

---

## 技术栈

### 后端 (Python 3.12+)
| 组件 | 技术 | 版本/备注 |
|------|------|-----------|
| 框架 | FastAPI | 全异步 |
| ORM | SQLAlchemy 2.0 (async) | `async_sessionmaker` |
| 数据库 | SQLite (开发) / PostgreSQL 16 (生产) | docker-compose |
| 缓存 | Redis 7 | docker-compose |
| 校验 | Pydantic v2 | `from_attributes=True` |
| LLM | DeepSeek (默认) / Anthropic / OpenAI | 通过 `BaseLLMAdapter` 抽象 |
| 测试 | pytest + pytest-asyncio | `asyncio_mode = auto` |

### 前端 (TypeScript)
| 组件 | 技术 | 版本 |
|------|------|------|
| 框架 | Next.js (App Router) | **16.2.9** |
| React | | **19.2.4** |
| 样式 | Tailwind CSS | **v4** (PostCSS 插件) |
| UI 库 | shadcn/ui + Base UI | style: "base-nova" |
| 状态管理 | Zustand | ^5 |
| 图标 | lucide-react | ^1.21 |
| i18n | 自定义 Context | en / zh |

---

## 关键约束

### ⚠️ Next.js 16 破坏性变更
**在编写任何前端代码之前**，必须先阅读 `frontend/AGENTS.md`。Next.js 16 的 API、约定和文件结构与训练数据中的 Next.js 有重大差异。前端目录下的 `frontend/CLAUDE.md` 引用 `@AGENTS.md`，同样适用。

### 后端安全
- **所有用户文本输入** 必须经过 HTML 标签剥离：`re.sub(r"<[^>]*>", "", v).strip()`
- **API Key 绝不硬编码** — 使用 `.env` 环境变量
- **`.githooks/pre-commit`** 阻止任何 API key 被提交到仓库
- `.claude/` 目录已在 `.gitignore` 中排除

### 认证模式
- 双令牌制：`admin_token` + `candidate_token`（`secrets.token_urlsafe(32)` 生成）
- 管理员：请求头 `X-Admin-Token`
- 候选人：查询参数 `?token=...`
- 可选全局令牌：`MASTER_ADMIN_TOKEN`（为空则跳过验证）
- **没有登录页面、没有 session/cookie、没有 JWT**

---

## 后端编码约定

### 异步模式
```python
# 数据库访问 — 始终使用 async
async def get_sessions(db: AsyncSession) -> list[InterviewSession]:
    result = await db.execute(select(InterviewSession).order_by(...))
    return result.scalars().all()

# LLM 调用 — 始终使用 async
response = await self._llm.generate(prompt, system_prompt)
```

### 项目结构
```
backend/app/
├── api/          # FastAPI 路由 (REST + WebSocket)
├── core/         # 业务逻辑 (agent, fsm, conversation, evaluator)
├── db/           # SQLAlchemy 引擎 + seed 数据
├── llm/          # LLM 适配器抽象层
│   └── prompts/  # 系统提示词模板
├── models/       # SQLAlchemy ORM 模型
├── schemas/      # Pydantic v2 校验模式
└── services/     # 业务服务 (question_bank, report)
```

### FSM 状态机 (`backend/app/core/fsm.py`)
- **不要修改此文件** 除非用户明确要求
- 6 个状态：`IDLE → INTRO → QA_LOOP → WRAPUP → DONE`，外加 `PAUSED`（断线恢复）
- 会话恢复数据序列化在 `metadata_json["paused_state"]` 中

### 数据库
- 所有模型使用 UUID 主键：`id = Column(String(36), default=lambda: str(uuid.uuid4()))`
- 时区感知时间戳：`datetime.now(timezone.utc)`
- 级联删除：`cascade="all, delete-orphan"`
- 软删除用于问题库和职位：`is_active=False` / `status="archived"`
- seed 数据是幂等的（表不为空则跳过）
- Alembic 用于迁移，不要手动改表结构

### LLM JSON 解析
评估结果解析需要**多级回退策略**：
1. 直接 `json.loads()`
2. 移除 markdown 代码围栏后解析
3. 大括号提取 `re.search(r'\{.*\}', text, re.DOTALL)`
4. 正则表达式修复尾随逗号
5. 针对 score/comment 的正则表达式最终回退
6. 完全失败时使用 `_fallback_result`（默认分数 3）

### 日志
- 使用 `logging.getLogger(__name__)`
- 轮换文件日志：`logs/interview.log`，10MB 上限，保留 5 个备份

---

## 前端编码约定

### 组件模式
- 所有页面使用 `"use client"` 指令
- 使用 `useSearchParams()` 的页面必须包裹在 `<Suspense>` 中
- 动态路由参数是 Promise：`params: Promise<{ sessionId: string }>`，用 `use(params)` 解包
- shadcn/ui 组件基于 **Base UI 原语**（如 `@base-ui/react/button`），不是 Radix
- Dialog 使用 `render` prop 模式（Base UI 风格），非 `asChild`
- 类名合并：使用 `cn()` from `@/lib/utils`（clsx + tailwind-merge）

### 状态管理
- **Zustand** (`useInterviewStore`) — 唯一的全局状态，用于面试实时数据
- 其他页面使用本地 `useState`
- **无表单库** — 所有表单手动管理受控输入和验证
- 持久化到 `localStorage`：主题、语言、会话令牌 (key: `admin_${sessionId}` / `candidate_${sessionId}`)

### WebSocket (`src/hooks/use-websocket.ts`)
- 原生 WebSocket API（无 Socket.IO）
- 指数退避自动重连（`MAX_RECONNECT_ATTEMPTS`, `RECONNECT_BASE_DELAY_MS`）
- 7 种服务端消息类型通过 `dispatchRef` 分发
- 发送消息时乐观追加本地 `Message`

### i18n
- 自定义 Context 系统（非 react-i18next）
- 翻译文件：`src/i18n/locales/en.json` 和 `zh.json`
- 嵌套访问：`t.dashboard.title`, `t.interview.waiting`
- 语言偏好持久化在 `localStorage("app_locale")`

### API 调用 (`src/lib/api.ts`)
- 通用 `request<T>()` 包装器，自动设置 `Content-Type: application/json`
- 自定义 `ApiError` 类，包含 HTTP 状态码
- 无请求缓存、去重或重试逻辑 — 遵循现有模式

---

## 安全要求

1. **XSS 防护**：所有用户文本输入必须 strip HTML 标签
2. **API Key 保护**：`.githooks/pre-commit` 阻止 `sk-*`、`sk-ant-*`、`AIza*` 等模式被提交
3. **令牌安全**：会话令牌通过 `secrets.token_urlsafe()` 生成
4. **CORS**：仅允许 `localhost:3000` 和 `127.0.0.1:3000`
5. **`.env` 绝不提交**（已在 `.gitignore` 中）
6. **认证检查**：每个需要保护的端点必须使用 `Depends(verify_admin_token)` 或 `Depends(verify_candidate_token)`

---

## 常用命令

```bash
# 后端
cd backend && uvicorn app.main:app --reload          # 启动开发服务器
cd backend && python -m pytest tests/ -v              # 运行所有测试
cd backend && pip install -r requirements.txt         # 安装依赖

# 前端
cd frontend && npm run dev                            # 启动开发服务器
cd frontend && npm run build                          # 生产构建
cd frontend && npm run lint                           # ESLint 检查

# Docker（生产环境 PostgreSQL + Redis）
docker compose up -d                                  # 启动数据库服务
```

## 环境变量（关键项）

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `LLM_PROVIDER` | `deepseek` | `anthropic` / `deepseek` / `openai` |
| `DATABASE_URL` | `sqlite+aiosqlite:///interview.db` | 生产用 PostgreSQL |
| `DEFAULT_TOTAL_QUESTIONS` | `5` | 每场面试题目数 |
| `ANSWER_TIMEOUT_SECONDS` | `120` | 单题答题超时 |
| `MAX_SKIP_COUNT` | `3` | 最多跳过次数 |
| `SLIDING_WINDOW_SIZE` | `10` | 对话历史窗口 |
| `MASTER_ADMIN_TOKEN` | `""` | 为空则跳过管理认证 |

---

## 测试约定

```python
# 所有测试使用内存 SQLite（test_engine fixture）
# 所有测试自动 asyncio（asyncio_mode = auto）
# MockLLM 返回预设 JSON，无网络调用

# 运行单个测试文件
python -m pytest tests/test_api.py -v --tb=short
# 运行特定测试
python -m pytest tests/test_api.py::TestRESTEndpoints::test_create_session -v --tb=short
```

- `testpaths = ["tests"]`
- 每个模块通过 `setup_test_db` fixture 隔离数据库
- FSM 测试覆盖所有合法和非法转换
- LLM 回退测试覆盖畸形 JSON 场景

---

## 不要做的事

- ❌ 不要修改 `backend/app/core/fsm.py` 的状态转换（除非明确要求）
- ❌ 不要在 LLM 评估解析中去掉回退策略
- ❌ 不要在前端引入 react-hook-form / axios / react-query 等新库（项目有既定模式）
- ❌ 不要用 Radix 替代 Base UI 原语
- ❌ 不要在 `.env` 中硬编码 API key 并提交
- ❌ 不要手动修改数据库表结构（使用 Alembic 迁移）
- ❌ 不要在 `useSearchParams()` 外不加 `<Suspense>`
- ❌ 不要创建没有 input 消毒的新 Pydantic schema

---

## 相关文件

- `frontend/CLAUDE.md` + `frontend/AGENTS.md` — Next.js 16 破坏性变更警告
- `.claude/settings.local.json` — 本地命令权限白名单
- `.githooks/pre-commit` — API key 泄露防护
- `.env.example` — 完整环境变量模板
- `README.md` — 项目架构文档
